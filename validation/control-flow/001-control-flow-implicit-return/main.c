int main(int argc, char** argv) {
    // The compiler will automatically insert a return statement (which returns 0) in the main function
    // if control flow reaches the end of the function without a return statement.
}
